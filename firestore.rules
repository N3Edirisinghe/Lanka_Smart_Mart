rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // Allow read/write access until the end of 2026
      allow read, write: if request.time < timestamp.date(2027, 1, 1);
    }
  }
}
